@extends('layouts.app') @section('title','Excuse Requests') @section('content')
<div class="flex items-end justify-between"><div><h1 class="text-3xl font-bold text-slate-900">Excuse Requests</h1><p class="mt-2 text-slate-500">Search, filter, and track requests.</p></div>@if(auth()->user()->role==='student')<a href="{{ route('requests.create') }}" class="rounded-xl bg-[#B3262E] px-5 py-3 font-semibold text-white">+ New request</a>@endif</div>
<form class="mt-7 grid gap-3 rounded-2xl border bg-white p-4 shadow-sm sm:grid-cols-[1fr_220px_auto]"><input name="search" value="{{ request('search') }}" placeholder="Search reference or student…"><select name="status"><option value="">All statuses</option>@foreach(['draft','submitted','under_review','returned','approved','rejected','acknowledged','completed'] as $s)<option value="{{ $s }}" @selected(request('status')===$s)>{{ ucwords(str_replace('_',' ',$s)) }}</option>@endforeach</select><button class="rounded-xl bg-[#123A63] px-6 font-semibold text-white">Filter</button></form>
<div class="mt-5 overflow-hidden rounded-2xl border bg-white shadow-sm">@include('requests._table',['items'=>$requests])</div><div class="mt-5">{{ $requests->links() }}</div>
@endsection
